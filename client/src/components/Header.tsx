export function Header() {
  return (
    <header className="bg-white border-b border-gray-200 px-6 py-2" data-testid="header">
      {/* Header is now minimal - main branding and controls moved to sidebar */}
    </header>
  );
}
